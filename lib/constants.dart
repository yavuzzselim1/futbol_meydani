import 'package:flutter/material.dart';

// ─── Renk Sabitleri ────────────────────────────────────────────────
const Color bg = Color(0xFF030906);
const Color panel = Color(0xFF0B2119);
const Color panel2 = Color(0xFF103025);
const Color green = Color(0xFF71F39A);
const Color muted = Color(0xFFAFC4B6);

// ─── Ses Efekti Enum ────────────────────────────────────────────────
enum GameSound {
  tap,
  select,
  lock,
  warning,
  countdown,
  reveal,
  win,
  perfect,
  start,
  home,
  refresh,
  exit,
}
